import 'package:flutter/material.dart';

class PayStripeView extends StatelessWidget{
    PayStripeView({super.key,required this.oid});
String oid;
  @override
  Widget build(BuildContext context) {
     return  const SafeArea(
       child: Scaffold(

       ),
     );
  }

}